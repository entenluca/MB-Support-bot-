# Pflicht (einer der drei Namen funktioniert)
BOT_TOKEN=DEIN_DISCORD_BOT_TOKEN
# DISCORD_TOKEN=DEIN_DISCORD_BOT_TOKEN
# TOKEN=DEIN_DISCORD_BOT_TOKEN

VOICE_CHANNEL_ID=DEINE_VOICE_CHANNEL_ID

# Optional: Falls keine Channel-ID gesetzt ist, wird nach diesem Namen gesucht.
VOICE_CHANNEL_NAME=Warteraum

# ANTENNE 1 Stream
RADIO_URL=https://streams.antenne1.de/a1stg/mp3-128/streams.antenne1.de/

# Optional: Nur setzen, wenn dein Hoster einen eigenen FFmpeg-Pfad vorgibt.
# Standard: System-FFmpeg -> automatisch ffmpeg-static als Fallback.
FFMPEG_BIN=
